import transport.Bus;

public class DriverCategoryC extends Driver<Bus> {

    public DriverCategoryC(String fullName, String typeLicense, int workExperience, Bus car) {
        super(fullName, typeLicense, workExperience, car);
    }
}
